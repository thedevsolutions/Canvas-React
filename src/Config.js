// server URL for the application
const server = 'http://localhost:8000';
export const apiUploadImage = `${server}/uploads`; // route to upload image to the server
export const apiImagesList = `${server}/images`; // route to fetch list of images